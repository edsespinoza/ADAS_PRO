{
  "name": "adas-pro-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.js",
    "dev": "node scripts/build-config.js --dev && npx serve ."
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
